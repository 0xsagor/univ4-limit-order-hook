# univ4-limit-order-hook
An advanced Uniswap V4 Hook implementation that enables on-chain limit orders. Uses the 'beforeSwap' and 'afterSwap' entry points to manage and execute orders based on real-time price ticks.
